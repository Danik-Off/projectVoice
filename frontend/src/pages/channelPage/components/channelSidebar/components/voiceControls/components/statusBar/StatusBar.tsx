import React from 'react';
import './StatusBar.scss';

const StatusBar: React.FC = () => {
    return (
        <div className="status-bar">
            <span className="status-bar__message">test status</span>
        </div>
    );
};

export default StatusBar;
