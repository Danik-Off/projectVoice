import React from 'react';

const StatusBar: React.FC = () => {
    return (
        <div className="status-bar">
            <span className="status-message">test status</span>
        </div>
    );
};

export default StatusBar;
